Build Instructions: Aquila AM69 with TI Staging Kernel
=======================================================

This layer (meta-aquila-ti-staging) extends the standard Toradex BSP for the
Aquila AM69 module to use the upstream TI staging kernel (linux-ti-staging 6.6)
in place of Toradex's downstream fork (linux-toradex-ti).

All other BSP components (U-Boot, GPU drivers, firmware) remain as configured
by the standard Toradex BSP.


Prerequisites
-------------

Install the repo tool and standard Yocto host dependencies.  See:
https://developer.toradex.com/linux-bsp/in-depth/build-yocto-image-for-toradex-modules/

On Ubuntu/Debian:

    sudo apt install repo gawk wget git diffstat unzip texinfo gcc build-essential \
        chrpath socat cpio python3 python3-pip python3-pexpect xz-utils debianutils \
        iputils-ping python3-git python3-jinja2 libegl1-mesa libsdl1.2-dev \
        pylint xterm python3-subunit mesa-common-dev zstd liblz4-tool


1. Initialize the Toradex BSP workspace
----------------------------------------

    mkdir -p ~/yocto/scarthgap
    cd ~/yocto/scarthgap

    repo init \
        -u https://github.com/toradex/toradex-bsp-platform.git \
        -b scarthgap \
        -m tdxref/default.xml

    repo sync


2. Clone this layer
-------------------

    # Replace <TBD_REPO_URL> with the actual git URL for meta-aquila-ti-staging
    git clone <TBD_REPO_URL> layers/meta-aquila-ti-staging


3. Set up the build environment
--------------------------------

Source the export script from the workspace root.  On the first run it copies
the template bblayers.conf and local.conf into build/conf/.

    cd ~/yocto/scarthgap
    source export


4. Add the meta-aquila-ti-staging layer
----------------------------------------

Run this from within the build directory (the shell is already there after
sourcing export):

    bitbake-layers add-layer ../layers/meta-aquila-ti-staging


5. Configure the build
-----------------------

Edit build/conf/local.conf and set at minimum:

    MACHINE = "aquila-am69"
    ACCEPT_FSL_EULA = "1"

Optional — set a shared download directory to avoid re-fetching sources across
builds:

    DL_DIR = "/path/to/shared/downloads"


6. Build an image
------------------

    bitbake tdx-reference-minimal-image

Other supported images:

    tdx-reference-multimedia-image
    core-image-base


Notes
-----

- The TI staging kernel (linux-ti-staging 6.6) is provided by the
  meta-ti/meta-ti-bsp layer, which is already included in the standard Toradex
  BSP repo manifest.

- The bootloader (u-boot-ti-staging) and TI BSP component versions are
  determined by TI_PREFERRED_BSP = "ti-6_6" set in the aquila-am69 machine
  configuration and are not changed by this layer.

- To revert to the Toradex downstream kernel, simply remove this layer:

      bitbake-layers remove-layer ../layers/meta-aquila-ti-staging
